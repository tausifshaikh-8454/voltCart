

const Button = ({
    text = "Button",
    borderClr = 'border-prim-blue',
    bgClr = "bg-prim-blue",
    textClr = "text-white",
    hoverBgClr = "bg-white",
    hoverTextClr = "text-black",
    btnWidth = "w-100",
    handlerClickBtnComp,
    type,
}) => {
    return (
        <button
            onClick={handlerClickBtnComp}
            type={type}
            className={` ${btnWidth}  uppercase max-w-100 transition-all flex justify-center items-center p-[12px 25px] px-[25px] py-[12px] border ${borderClr} ${bgClr} ${textClr} hover:${hoverBgClr} hover:${hoverTextClr} cursor-pointer rounded-[6px]  `} >
            {text}
        </button>
    )
}

export default Button;
